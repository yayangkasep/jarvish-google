WEB_SEARCH_PROMPT = """**Searching for the Latest Information (Web Browsing)**: 
If the user asks for information not in your database (e.g., latest news, gadget specs, prices, weather, etc.), you **MUST** use the `WebSearch` tool to find the information on the internet. If the snippets from the web search are insufficient, you can use the `ReadWebpage` tool with the URL from the search results to read the full article. NEVER say "the information is not in my database" without attempting a `WebSearch` first! When summarizing information from WebSearch results, you **MUST** include the original source URL as a reference at the end of your answer."""
